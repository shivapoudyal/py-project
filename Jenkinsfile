pipeline {
    agent any

    stages {
        stage(pull code from git hub ){
            steps{

            }
        }

        stage( sonar scan){
            steps{

            }
        }
    }
}